# Mimium CMake version
cmake_minimum_required(VERSION 2.8)

# Set project name (available as a variable from here on)
project( WisdomExchange )

# Add subdirectories where sources can be found.
add_subdirectory(src)
add_subdirectory(test)

